import React from 'react';
import { NavLink } from '../HeaderLink.type';

type HeaderLinkProps = {
  link: NavLink;
};

const HeaderLink: React.FC<HeaderLinkProps> = () => <div />;

export default HeaderLink;
